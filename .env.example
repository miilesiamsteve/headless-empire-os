# Local secrets template for YOUR private OS clone.
# Copy to .env and fill in. Never commit .env.

# Example placeholders only — use the tools you actually run:
# POWERLOBSTER_API_KEY=
# GFAVIP_SESSION=
# OPENAI_API_KEY=
